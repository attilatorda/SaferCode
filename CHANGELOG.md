# Changelog

## [0.9.1] - 2026-05-15

- Added explicit **experimental status** warning to project documentation.
- Enhanced `sc_log.h` with **HTTP (REST-style) logging** via `LOG_HTTP`.
- Added configurable logging fallback (`log_set_fallback` / `log_clear_fallback`) to support scenarios like network failure -> file logging.

## [0.9.0] - 2026-04-29

First public release.
