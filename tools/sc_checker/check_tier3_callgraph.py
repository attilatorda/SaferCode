"""Tier3 callgraph-style checker placeholder."""

from pathlib import Path


def run(target: Path) -> list[dict]:
    # Future: deeper inter-procedural / callgraph checks.
    _ = target
    return []
