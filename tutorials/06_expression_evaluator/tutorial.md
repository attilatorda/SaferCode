# Tutorial: Expression Evaluator (Arena AST)

## Goal

Parse arithmetic expressions into an arena-allocated AST, evaluate it, then free all temporary state with one reset.

## Why this belongs in tutorials

It combines parsing, recursive structures, and lifetime design—too deep for a short copy-paste example.

## Full solution

See `solution.c`.
